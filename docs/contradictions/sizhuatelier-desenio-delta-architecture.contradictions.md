# Contradiction Ledger — SizhuAtelier Desenio Delta Architecture

Feature Slug: `sizhuatelier-desenio-delta-architecture` · Branch: `feat/desenio-delta` · Mode: CORE-manual

> Audit trail of True-Line / Plumbline-Watcher contradictions and their resolutions.
> A contradiction may only be resolved by an allowed resolution the **user** confirms —
> never by mock, placeholder, laundering, agent consensus, or completion pressure.

---

## CONTRA-DELTA-001 — Canvas confirmation-status contradiction — **RESOLVED 2026-06-27**

- **Raised by:** Plumbline-Watcher, Phase 2 / Milestone 1 gate (verdict `pause`, true-line-status `contradiction`).
- **Finding:** The Product Canvas contradicted itself on confirmation status. Header (lines 3, 6) and the traceability/state docs asserted `user-confirmed (RE-CONFIRMED ... final)`, while the Canvas body (council-rule record + "User Confirmation" section) still said the amended Canvas had returned to `draft`, that the prior confirmation "reicht NICHT als Re-Bestätigung des amendierten Canvas", and "Eine erneute, ausdrückliche Nutzerbestätigung steht aus". Both cannot be true.
- **Escalation rule applied:** Only the user may reclassify a confirmation status; no agent self-confirm. The orchestrator did **not** rewrite the status on its own authority.
- **Resolution (document-conformant proof):** Verified against the conversation transcript that the user typed the **exact** confirmation phrase ("Ich bestätige, dass Product Canvas und Product Vision … verwendet werden dürfen") a **second time** (transcript line 1522) **after** the council amendments were applied to the Canvas (amendments applied + "nothing confirmed" reported at lines 1492–1501), immediately followed by "REQ-002 inkrementell akzeptiert, GO, phase 1 go" (line 1564). That second entry is the governing re-confirmation of the amended Canvas; the "reicht NICHT / steht aus" note predated it and was stale.
- **Action taken:** Canvas header and body reconciled to a single consistent status `user-confirmed` (amended fassung, RE-CONFIRMED 2026-06-27). Stale "draft/awaiting" passages either corrected or explicitly framed as historical/outdated. An honest evidence note was added citing the transcript. No new product decision was made — status consistency only.
- **PRD note:** The PRD was **not** flipped. It remains `ready-for-user-confirmation`; the user confirmed Canvas + Vision only, and has not yet explicitly confirmed the PRD.
- **Independent re-check:** Plumbline-Watcher remediation re-check returned `pass` (contraResolved=true, prdCorrectlyUnflipped=true). Orchestrator independently verified header==body and the PRD status against the real files.

---

## CONTRA-DELTA-002 — M2 navigation lands buyers on a live "Coming Soon" Patron-Fold / Credits block — **RESOLVED 2026-06-27**

- **Raised by:** Plumbline-Watcher, Milestone 2 (Navigation & Header) True-Line / Canvas gate. Verdict `pause`, true-line-status `contradiction`.
- **Finding:** The M2 navigation work (`src/components/Navbar.tsx`) routes buyers to `/collections` from the primary-nav "Poster" entry (`PRIMARY_NAV` `{ href: '/collections' }`), the mega-menu "Alle Kollektionen" link, and the mobile drawer "All Posters" link. `/collections` renders the LIVE component `src/pages/Kollektion.tsx`, whose lines 71–78 render a "Patron Vault — coming soon" section: eyebrow `coll.patron.eyebrow` = "Coming soon" / "Bald verfügbar" / "Bientôt disponible", title "Patron Vault", desc referencing "Celestial Credits" (`src/i18n/translations.ts:115, 421, 726`). It is not behind any flag (unlike `REVIEWS_ENABLED` / `COMMERCE_ENABLED`); it renders unconditionally.
- **Why this threatens customer value:** This is exactly the trust-eroding surface the confirmed baseline requires removed — VIS-021 ("Fake Reviews, Credits, Coming Soon und Patron Fold sind aus der sichtbaren Shop-UI entfernt"), CAN-014 (removal of Coming Soon / Patron Fold / credit system / productive placeholders), Non-Goal CAN-017 (no credits in the current MVP), Non-Goal CAN-018, and REQ-010 / TR-010. The M2 navigation makes this worse by wiring the shop's primary "Poster" destination and mega-menu directly into the page that still shows it. Traceability TR-010 is marked `aligned`/clean while the running code on that landing page contradicts it.
- **Escalation rule (no self-downgrade):** This is a "Coming Soon / Patron Fold / Credits not removed" finding against a confirmed Non-Goal and an explicit removal requirement. It may NOT be self-downgraded to a "known limitation". It is NOT covered by the only `/collections` RED-carry in the evidence ledger (RL-CHROMIUM, which concerns UNRUN Playwright specs on a real browser, not the Patron Fold). Only the user may reclassify.
- **Required user decision:** Either (a) confirm the Patron Vault / "Coming Soon" / Celestial-Credits block must be REMOVED from `Kollektion.tsx` (+ its `coll.patron` i18n) as part of — or as an immediate follow-up gating — the M2 navigation work before M2 can be considered true-to-value, OR (b) explicitly reframe scope (e.g. defer the removal to a named later milestone with the nav temporarily NOT pointing the primary "Poster"/mega entry at a page that still shows it). No mock/placeholder/laundering resolution is permitted.
- **Forbidden shortcut:** Declaring it a "known limitation", hiding it behind an undisclosed flag without removal, or treating RL-CHROMIUM as if it already covered this. *(Note retained as audit trail: none of these shortcuts was used in the resolution below.)*
- **Resolution (allowed — option (a), user-prioritised removal) 2026-06-27:** The user prioritised the Trust-Cleanup ahead of the deferred home-resequence. The offending surfaces were **actually removed** from the visible shop UI (no mock / no placeholder / no undisclosed-flag / no laundering / no "known limitation"):
  - **Patron-Fold / Coming-Soon / Celestial-Vault removed from the `/collections` hub** (the hub page now renders only legitimate content: collection cards, the all-posters grid, FAQ, newsletter — no `coll.patron` fold, no "Coming soon / Bald verfügbar / Bientôt disponible", no "Vault" heading).
  - **Celestial-Credits removed across Cart / Checkout / PDP / Personalize / Account** (no earn line, no per-line `+N C.` badge, no "save your Celestial Credits" upsell — the neutral sign-in prompt takes its place — no credits-balance card; a legacy persisted `creditsEarned` value renders nothing).
  - **Fake-Reviews / Sterne gated** on Cards + PDP behind the same `REVIEWS_ENABLED && reviews>0` switch (review module stays OFF until OQ-004).
- **Residual-surface test-gap CLOSED (2026-06-27, the three areas the Watcher caught as `governanceConsistent=false`):** the earlier wave declared this `resolved … incl. Account` while three credit/vault rest-surfaces still rendered. Re-verified independently against the real files; all three are now genuinely clean **and** asserted by the credits-removed spec so a re-introduction goes RED:
  - **Persistent header badge** — `src/components/Navbar.tsx`: the header account control is a plain `User` icon → `/account`; **no `{user.points} C.` badge**. Asserted on the `<header role="banner">` (it persists on every route, so a leak there is global).
  - **Signed-out auth subtitle** — `src/pages/Account.tsx` via `auth.subtitle` (`src/i18n/translations.ts`): EN/DE/FR are the neutral "Sign in to view your orders and account." with **no Vault/credits language**; the logged-in dashboard renders **no Celestial-Credits balance card**.
  - **Legal clause** — `src/lib/legal.ts`: the Privacy Policy lists the real processing purposes with **no Celestial-Credits clause** in any language.
- **Verification (TDD + Mutation + Sweep):** real-boundary-jsdom over the **real** `src/App.tsx` composition root with explicit mutation guards — `tests/unit/delta-collections-hub-trust-cleanup.test.tsx`, `tests/unit/delta-celestial-credits-removed.test.tsx` (now also mounts the persistent header, the signed-out auth screen and the Legal page and asserts `CREDITS_OR_VAULT` absence on each), `tests/unit/delta-card-review-gating.test.tsx` (**24 PASS / 0 FAIL**, `vitest run --isolate=false` — re-run 2026-06-27; was 21 before the header/auth/legal residual cases closed the gap). A deliberate re-introduction of any Patron/Coming-Soon/Credits/Vault surface (including the header badge, the auth subtitle, or a legal clause) turns a case RED (mutation evidence). A shipped-scan over the real imported modules confirms the absence. Evidence: `docs/reality/sizhuatelier-desenio-delta-architecture.evidence.jsonl` (REQ-010 line, `contradiction_id: CONTRA-DELTA-002`).
- **Backend credits roll-back — USER-AUTHORISED + DONE 2026-06-27 (updated; supersedes the earlier "separate, out-of-scope, NOT resolved" note):** when this finding was first written, the credits *machinery* still lived in the server/auth layer and was deliberately untouched by the UI-removal. **On 2026-06-27 the user explicitly authorised the backend credits roll-back, and it is now implemented and verified** (verified by context-keeper against the real files 2026-06-27):
  - `server/index.js`: `recordCreditsEarned` (the sole `credits_ledger` writer) **removed**; `WELCOME_CREDITS = 20` welcome grant **removed**; the earn-on-order / REQ-045 earn path **removed** (no active credits writer or reader remains — grep confirms zero live `recordCreditsEarned`/`WELCOME_CREDITS` assignments).
  - `/api/auth/me`: **no longer returns** `points_balance`/`lifetime_points`/`unlocked_features`/`achievements` (credits dropped from the auth response).
  - `src/lib/auth.ts`: the `points`/`lifetime`/`unlockedFeatures`/`achievements` fields on `AuthUser` are **removed** from the contract (only a decommission comment remains).
  - The stale "Celestial Vault" comment in `src/components/shop/NewsletterSection.tsx` is **gone**.
  - **Money path unaffected** (pricing/checkout repricing untouched; repricing integration tests stay green — 400/400 `npm test` per the state ledger).
  - **EINZIGER verbleibender Folge-Punkt (the only open follow-up): the separate DB-DROP migration.** The dormant Postgres schema still exists — `server/index.js` still issues `CREATE TABLE IF NOT EXISTS credits_ledger` and keeps the `points_balance`/`lifetime_points`/`credits_reserved` columns, explicitly marked DORMANT/DECOMMISSIONED and never read or written. Dropping these tables/columns is a separate, scheduled migration; the dormant remnants are not customer-facing and write/read nothing. This follow-up is honestly OPEN; it does not re-open CONTRA-DELTA-002 (no rendered or live credits surface remains).
- **Honesty bound:** This is **`test-verified` in-sandbox only**, `wired_in_prod=no` (delta uncommitted on `feat/personalization-first-mvp`, not through the deployed composition root). It is **NOT** production-verified and must never be reported as such; the RED-for-confidence carry stays visible until a deployed real-boundary smoke wires it through production.
- **Traceability reconciliation:** TR-010 / REQ-010 in `docs/traceability.md` reflects the honest implemented state (`evidence-class: unit-test`, `production-verification-status: test-verified`, `wired_in_prod=no`, NOT production-verified), now notes the three residual surfaces (header badge / auth subtitle / legal clause) are removed + the test-gap is closed at 24 PASS, and references `CONTRA-DELTA-002`. The matrix's Contradiction-Ledger section lists this CONTRA-id.
- **PRD note (updated 2026-06-27):** At the time of the M2 resolution the PRD was deliberately left unflipped. **It has since been user-confirmed on 2026-06-27** (exact user phrase recorded in `docs/prd/sizhuatelier-desenio-delta-architecture.prd.md` Z.3) — Canvas + Vision + PRD are now all `user-confirmed`. This product-context confirmation does NOT change the production-verified count (still **0**) and does not auto-accept any delivery row.
- **Status:** **RESOLVED 2026-06-27** by an allowed resolution the user prioritised (changed implementation — removal — verified by TDD + mutation + sweep, incl. the previously-missed header badge / auth subtitle / legal clause now asserted). The backend credits roll-back, originally logged as a separate out-of-scope follow-up, has since been **user-authorised and implemented (2026-06-27)**; the **only** remaining open follow-up is the separate DB-DROP migration of the dormant tables/columns (see the updated backend note above). M2 True-Line verdict released from `pause`. Production-verified count across the feature remains **0**. (The earlier premature "resolved incl. Account" claim — caught by the Watcher as `governanceConsistent=false` — is now genuinely true after independent re-verification.)
